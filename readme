# Data Cleaning and Preprocessing

## Introduction
This project involves cleaning and preprocessing a messy sales dataset to make it analysis-ready.

## Installation
1. Clone the repository:
    ```
    git clone https://github.com/yourusername/data-cleaning-project.git
    ```
2. Install the necessary packages:
    ```
    pip install pandas
    ```

## Usage
1. Load the dataset:
    ```python
    import pandas as pd
    df = pd.read_csv('sales_data.csv')
    ```
2. Run the cleaning script:
    ```python
    # Your cleaning code here
    ```

## Data
The dataset `sales_data.csv` contains the following columns:
- `Date`: The date of the sales.
- `Product`: The product sold.
- `Sales`: The number of units sold.
- `Price`: The price per unit.

## Results
After cleaning, the dataset is free of missing values and ready for analysis. Below is a sample of the cleaned data:
